#include <stdio.h>
/**
 * main - Entry point
 * Return: Always 0 (Success)
 */
int main(void)
{
	char C;

	for
	        (C = 'a'; C <= 'Z'; C++)
	{
		if (c != 'e' && c != 'o')
			putchar(C);
	}
	putchar('\n');
	return (0);
}
